# Copy to .env and fill in. NEVER commit real keys.
# Optional — only used by the LLM "advisor" controller that narrates decisions.
# The system runs fully without these (the RL agent needs no API keys).
GROQ_API_KEY=
GEMINI_API_KEY=
